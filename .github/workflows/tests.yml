name: Tests

on: [push]

jobs:
  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v3

      - name: Run Tests
        uses: actions-rs/cargo@v1
        with:
          command: test
